<%= render 'post', post: @post %>
<div class="text-center edit-links">
  <% if current_user.id == @post.user.id %>
      <%= link_to 'Edit Post', edit_post_path(@post), class: 'btn join-family-button' %>
  <% end %>
</div>