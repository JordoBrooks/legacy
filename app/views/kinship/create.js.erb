$('#join-family-button').attr('class', 'btn leave-family-button')
                        .text('Leave Family')
                        .attr('href', "/<%= @user.id %>/leave_family")
                        .attr('id', 'leave-family-button');
